---
title: "Introduction"
description: "A modern, flexible MDX documentation framework for Next.js"
icon: "book"
---

# @farming-labs/docs

A modern MDX documentation framework built on top of [Fumadocs](https://fumadocs.vercel.app). One config file, zero boilerplate, beautiful themes out of the box.

## Why @farming-labs/docs?

- **Zero boilerplate** — No layout files, no `[[...slug]]` wrappers, no `mdx-components.tsx`. Just write MDX.
- **One config** — Everything in `docs.config.tsx`: theme, colors, typography, icons, components, metadata.
- **Three themes** — Default, Darksharp, and Pixel Border. Or build your own with `createTheme()`.
- **Built-in search** — Full-text search powered by Orama. No API keys.
- **CLI scaffolding** — `npx @farming-labs/docs init` detects your framework, generates config, installs deps, starts dev.

## Packages

| Package | Description |
|---------|-------------|
| `@farming-labs/docs` | Core types, `defineDocs()`, `createTheme()`, `extendTheme()` |
| `@farming-labs/theme` | Theme presets, layout components, CSS presets |
| `@farming-labs/next` | Next.js adapter — `withDocs()`, MDX processing, search API |

## Quick Start

```bash
npx @farming-labs/docs init
```

Or install manually:

```bash
pnpm add @farming-labs/docs @farming-labs/theme @farming-labs/next
```

Then follow the [Installation](/docs/installation) guide.
