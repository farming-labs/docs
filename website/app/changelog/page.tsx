import type { Metadata } from "next";
import { Component } from "@/components/ui/interactive-changelogs";

export const metadata: Metadata = {
  title: "Changelog",
  description: "All notable changes to @farming-labs/docs",
};

export default function ChangelogPage() {
  return <Component />;
}
