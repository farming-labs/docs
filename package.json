{
  "name": "docs-framework-root",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm --filter '@farming-labs/*' run build",
    "dev": "pnpm --filter example-next dev",
    "test": "pnpm --filter @farming-labs/docs run test",
    "typecheck": "pnpm --filter '@farming-labs/*' run typecheck",
    "release": "bumpp",
    "release:latest": "bumpp --tag latest && pnpm --filter '@farming-labs/*' publish --access public --tag latest --no-git-checks",
    "release:beta": "bumpp --preid beta",
    "publish:beta": "pnpm --filter '@farming-labs/*' publish --access public --tag beta --no-git-checks",
    "publish:latest": "pnpm --filter '@farming-labs/*' publish --access public --no-git-checks",
    "lint": "oxlint .",
    "format": "oxfmt .",
    "format:check": "oxfmt --check .",
    "lint:fix": "oxlint --fix ."
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.1.18",
    "bumpp": "^10.4.1",
    "oxfmt": "^0.35.0",
    "oxlint": "^1.50.0",
    "playwright": "^1.58.2",
    "puppeteer": "^24.37.5",
    "tailwindcss": "^4.1.18",
    "typescript": "^5.9.3"
  }
}
