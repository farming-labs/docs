# Skyvern Mintlify Provider

This provider is a text-docs mirror of the upstream Skyvern Mintlify docs. It keeps the same
human-readable content and `docs.json` navigation shape.

Large media assets are excluded from the benchmark copy because the benchmark serves markdown to
implementation agents.
